import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lost.city.client',
  appName: '2004scape-client',
  webDir: 'public',
  server: {
    androidScheme: 'https'
  }
};

export default config;
